import { randomCharacterGenerator } from '../../../helpers/random-character-generator';
import { module, test } from 'qunit';

module('Unit | Helper | random character generator');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = randomCharacterGenerator(42);
  assert.ok(result);
});
